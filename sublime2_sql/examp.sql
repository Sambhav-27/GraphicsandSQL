use db_world;
-- create table student(
-- 	id int not null auto_increment primary key,
-- 	name varchar(10),
-- 	marks int,
-- 	clg varchar(20)
-- );

-- create table teacher(
-- 	id int not null auto_increment primary key,
-- 	name varchar(10),
-- 	clg varchar(20)
-- );
-- insert into student(name,marks, clg) values ('ram', 99, 'nsit'), ('laxman', 98, 'dce'),('john', 67, 'jnu');
-- insert into teacher(name, clg) values ('t1', 'dce'), ('t2', 'nsit');
-- insert into teacher(name, clg) values ('ram', 'dce');

select * from teacher;
select * from student;

-- select * from teacher
-- inner join
-- student
-- on teacher.clg = student.clg;
-- -- where teacher.name is NULL;

-- select * from teacher
-- limit 2;

